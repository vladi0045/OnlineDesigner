@using OnlineDesigner.Controllers
@model IEnumerable<OnlineDesigner.Models.User>

@{
    ViewData["Title"] = "Users";
}

<h1>Users</h1>
<div class="row">
    <div class="col-4">
        <form asp-action="Index">
            <div class="form-group">
                <label class="control-label">Filter:</label>
                <input type="text" name="searchString" class="form-control"/>
            </div>
            <div class="form-group">
                <label class="control-label">Filter by:</label>
                <select id="filter" name="filter" asp-items="@Html.GetEnumSelectList<UsersController.Filter>()" class="form-control"></select>
            </div>
            <hr/>
            <div class="form-group">
                <input type="submit" value="Search" class="btn btn-primary w-100"/>
            </div>
        </form>
        <hr/>
        <p>
            <a class="btn btn-primary w-100" asp-action="Create">Create New User</a>
        </p>
    </div>
    <div class="col-8">
        <table class="table">
            <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.UserName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Role)
                </th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => user.UserName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => user.Role)
                    </td>
                    <td>
                        <a class="btn btn-primary w-100 align-items-center" asp-action="Info" asp-route-id="@user.Id">View User Information</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>